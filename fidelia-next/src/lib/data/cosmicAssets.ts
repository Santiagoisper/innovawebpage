/** Earth orbit loop — sky plate behind the DNA intro (WebGL + copy sit above). */
export const GALAXY_SKY_VIDEO = "/earth-orbit-sky.mp4";

/** Static fallback / poster while video loads. */
export const GALAXY_SKY_IMAGE = "/galaxy-sky.webp";
